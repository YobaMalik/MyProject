package com.example.demo.Form;

import com.example.demo.PaspAddDoc.GetInfoFSVOM;
import com.example.demo.ServerFiles;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import javax.inject.Inject;
import javax.servlet.http.HttpServletRequest;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.HashMap;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

public class UserForm {
/*
    ServerFiles files=new ServerFiles();

    private Map<String,ByteArrayOutputStream> fileArray=new HashMap<>();


    public String createSOM(Map<String,ByteArrayOutputStream> fileArray) throws IOException {
        Map<String,ByteArrayOutputStream> somList=new ConcurrentHashMap<>();
        GetInfoFSVOM newZipArchive=new GetInfoFSVOM();
        newZipArchive.createDocs(fileArray,somList);
        this.files.writeToServer(somList,"zipSomReposrt.zip");
       // fileArray.clear();
        return "calcTime";
    }
*/
}
